import { ActionArrow } from "./ActionArrow";
import { SiteFooter } from "./SiteFooter";

/** Kept as the name the Providence pages already import. */
export const ProvidenceArrow = ActionArrow;

export function ProvidenceFooter() {
  return <SiteFooter />;
}
